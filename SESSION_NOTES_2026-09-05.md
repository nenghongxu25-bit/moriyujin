# 2026-09-05 会话记录

## 工作习惯与协作边界

- 用户会在 Laya 编辑器里自己创建、摆放、命名节点；代码不要擅自修改这些已有节点的位置、尺寸、布局、层级和样式。
- 对已经摆好的 UI 节点，代码只负责读取节点、设置 `text`、设置必要的 `visible/color`、绑定点击逻辑和刷新数据。
- 代码生成的新辅助节点可以由代码控制布局，但除非必要，不要新增 UI 节点。
- 用户明确说“别写代码”“先看有没有记录”时，只检查资料和现有代码，不做实现。
- 出问题时优先检查 prefab 节点名、大小写、可见区域、GList 裁剪、脚本误挂、点击命中区域。
- Laya 节点名区分大小写，例如 `Text_1` 和 `text_1` 不一样。
- GList 模板里的子节点如果超出 item/list 可见区域，编译运行后可能被裁剪，看起来像“没显示”。
- 改代码前要先读现有节点结构，不要凭空猜节点名。

## 今日主要改动文件

- `src/RoleEquipmentPanel.ts`
- `src/RolePanelConfig.ts`
- `src/RolePanelTypes.ts`
- `assets/prefab/role.lh`

## 代码结构调整

- `RoleEquipmentPanel.ts` 原本约 2489 行，已开始拆分。
- 新增 `RolePanelTypes.ts`，统一放角色页相关类型：
  - `StatKey`
  - `PanelStats`
  - `EquipmentData`
  - `SkillData`
  - `MoveData`
  - `SkillProgress`
  - `RoleData`
  - `AttributeCapData`
  - `AttributeCapStats`
- 新增 `RolePanelConfig.ts`，统一放静态配置：
  - 角色列表
  - 面板名
  - 六维名
  - 初始属性
  - 等级成长
  - 经脉名称、穴位、冲穴数值、通脉效果
  - 装备槽位、初始装备、背包装备
  - 功法分类、默认装备功法、功法数据
  - 属性上限表
- `RoleEquipmentPanel.ts` 保留 UI 查找、渲染、点击处理、数值计算流程。

## 角色基础数值规则

- 青槐 0 级初始六维：
  - 臂力 10
  - 技巧 10
  - 身法 10
  - 根骨 10
  - 内息 10
  - 定力 10
- 每提升 1 级获得 6 点自由属性点。
- 等级成长：
  - 攻击 +20/级
  - 防御 +20/级
  - 气血 +100/级
  - 命中 +5/级
  - 偏斜 +5/级
  - 招架 +5/级
  - 速度 +5/级
  - 内力 +10/级

## 自由属性点功能

- role 属性页新增 `ziyoudian` 文本，显示当前可自由分配点数。
- 属性值列表旁边的“加”按钮：
  - 有自由点时点击一次，对应一级属性临时 +1。
  - 自由点 -1。
  - 对应属性值显示高亮色。
- 点击“确认加点”：
  - 临时加点写入角色自身属性。
  - 高亮恢复正常。
  - 刷新一级属性、二级面板、自由点。
- 点击“重置加点”：
  - 只撤回当前未确认的临时加点。
  - 自由点退回。
  - 刷新显示。
- `queding/chongzhi` 两个按钮上误挂的 `CloseNodeButton` 已移除，避免点击后关闭 role 面板。

## 数值来源总公式

角色自身数值和外力数值必须分开。

```text
角色自身数值 =
  角色0级初始数值
+ 等级成长
+ 经脉提升
+ 属性点转化

局外最终面板 =
  角色自身数值 * 角色局外Buff
+ 装备数值 * 装备局外Buff
+ 功法数值 * 功法局外Buff
+ 局外固定加值

局内面板 =
  局外最终面板 * 局内Buff
+ 局内固定加值
```

- 二级数值就是公式算出来的局外最终面板，不允许没有来源的“偷偷写死数值”。
- battle 页面应读取 role 页面同一套数值规则，避免显示和实战不一致。

## 六维转化规则

当前一级属性到二级属性转化：

- 臂力 -> 攻击：`臂力 * 5`
- 根骨 -> 气血：`根骨 * 20`
- 技巧 -> 命中：`技巧 * 5`
- 技巧 -> 招架：`技巧 * 5`
- 身法 -> 偏斜：`身法 * 5`
- 身法 -> 速度：`身法 * 5`
- 定力 -> 抵抗：`定力 * 2.5`
- 根骨 -> 韧性：`根骨 * 1`
- 内息 -> 内力：`内息 * 10`
- 内息 -> 内力回复：`内息 / 2`

## 资质系统

天赋页 `tianfu` 当前约定：

- `list_1` 显示六维名：
  - 臂力
  - 技巧
  - 身法
  - 根骨
  - 内息
  - 定力
- `list_2` 显示每个属性资质经验：
  - `n/5000`
- `list_2` 每行的 `Text_1` 是“加”按钮。
- 点击一次“加”，对应属性资质经验 +50。
- 单项属性资质达到 `5000/5000` 后，该行“加”变灰。
- 属性资质满 5000 后，对应一级属性获得 1.3 倍加成。
  - 例如臂力本来 100，臂力资质满后，实际显示和参与转化为 130。
- 资质加成必须同时影响：
  - 一级属性显示
  - 二级属性转化
  - 后续战斗读取的面板

## 天赋解锁等级

- 五个级别用于解锁 `tianfu/list`，不是单项属性资质等级。
- `tianfu/list` 有 6 个格子。
- 初始只亮第 1 个。
- 天赋等级每升一级，多亮 1 个。
- 当前升级需求：
  - 凡 -> 下：2000
  - 下 -> 中：5000
  - 中 -> 上：10000
  - 上 -> 绝：15000
- 到最高档时应亮满 6 个格子，避免第 6 格永远无法解锁。

## 属性上限系统

- 每个角色都有不同的六维属性上限。
- 属性上限会随等级提升。
- 上限只影响“一级属性转化为二级属性”的效率，不压一级属性显示值。
- 超过上限后，超出部分按每 50 点一档衰减：

```text
上限内：100% 转化
超过 0-50：80% 转化
超过 50-100：60% 转化
超过 100-150：40% 转化
超过 150-200：20% 转化
超过 200 以上：0% 转化
```

示例：

```text
臂力上限 = 100
实际臂力 = 180
用于转化攻击的有效臂力 =
100 + 50 * 0.8 + 30 * 0.6 = 158
```

- 青槐当前先设为均衡上限：
  - 六维基础上限 120
  - 每级成长 +2
- 其他角色在 `ROLE_ATTRIBUTE_CAPS` 里先放了不同定位的临时上限，后续需要按角色设计继续精调。

## 功法规则

- 所有套路、内功、轻功数值处理：

```text
0重数值 = 满重数值 * 0.3
剩余 70% 数值分 10重、共 30层提升
每层修炼都加数值
突破不直接加数值，只用于进入下一重
第10重再修炼满3层后显示已满重
```

- 装备栏三个节点显示当前装备功法名字，不显示固定的“套路/内功/轻功”。
- 点击已学习功法列表后：
  - 如果未装备，激活按钮显示“激活”。
  - 点击激活后替换到上方对应装备栏。
  - 数值实装到角色面板。
  - 已装备时按钮显示“已激活”。

### 霸王诀

- 类型：内功
- 满重基础数值：
  - 气血 11500
  - 内力 3250
  - 内力回复 100
- 招式：
  - 3重解锁：霸王
  - 5重解锁：出锋
  - 7重解锁：镇势
  - 10重解锁：背水

### 霸王刀法

- 类型：套路
- 满重基础数值：
  - 攻击 3100
  - 命中 980
- 招式：
  - 霸气初现：主动，获得最大生命值 8% 临时护盾，并施加“力量”buff；力量增加自身 5% 臂力，可叠加 5 层，持续 25 调息，冷却 8 调息，消耗 300 内力。
  - 重楼：装备重楼时，攻击 +1550，命中 -100，普攻倍率 1.3。
  - 卸甲：每次出手获得“卸甲”buff，降低自身 5% 防御力，最多 6 层，消耗 25 内力。
  - 长歌：击败目标后立即再行动一次。
  - 无归：卸甲 buff 满 6 层时，下回合转化为一层“无归”buff：-30% 防御，+45% 攻击，+15% 穿甲，持续至战斗结束。

### 寒缨落月枪

- 1、3、5、7、10 重分别解锁 5 个招式。
- 未解锁招式显示灰色，解锁后显示亮色。

## 经脉规则

- 经脉每条的顺通/逆通方向必须独立记录。
- 每条经脉的顺通/逆通冲穴进度也必须独立。
- 点击顺通或逆通后：
  - 显示该方向的冲穴数值。
  - 显示该方向对应通脉效果。
  - 未全通时通脉效果灰色。
  - 通脉后通脉效果亮色。
- 切换页面、点击别的经脉再点回来，不能重置已选择方向和已冲穴进度。
- `role` 和 `jingmai` 页面都新增了 `node_5` 重置按钮。
- 点击重置可以改变当前经脉冲穴方向，但不返还已经消耗的真气。

## 装备页规则

- 装备类型 `list_1` 只展示，不显示高亮，不可点击。
- 装备列表 `list_2` 可以点击并显示高亮。
- 点击高亮位置要与实际格子一致，不能偏到别的格子。

## 阵型页规则

- `zhenxing` 预制体可以用 GList 显示九宫格阵位。
- 点击右侧角色：
  - 左侧 9 个格子都显示高亮。
  - 点击任意格子即可上阵。
  - 如果该格已有角色，则替换。
  - 被替换角色回到右侧角色列表。
- 点击左侧已上阵角色：
  - 显示“下阵”按钮。
  - 点击下阵后角色回到右侧角色列表。
- 点击 GList 外其他区域，取消当前锁定/选择。

## 战斗公式记录

- 局内伤害基础：

```text
基础伤害 = 局内攻击力 * 攻击倍率
```

- 防御免伤率：

```text
防御免伤率 =
有效防御 * 防御修正系数 / (基础伤害 + 有效防御 * 防御修正系数)
```

- 高倍率攻击会提高基础伤害，因此天然体现更强破防能力。

### 降防、穿甲、无视防御

```text
降防后防御 =
max(0, 目标局内防御 * (1 - 百分比降防) - 固定降防)

有效防御 =
max(0, 降防后防御 * (1 - 百分比穿甲 - 无视防御比例) - 固定穿甲)
```

- 降防：目标身上的 debuff，先改变目标防御。
- 穿甲：攻击者本次攻击属性，只降低本次攻击计算中的有效防御。
- 无视防御：特殊穿甲，通常来自技能机制或特殊状态。

### 穿甲和免伤上限

- 不设置全来源统一硬上限。
- 从武器之外的装备获取的最大穿甲值不超过 25%。
- 武器、功法、天赋、局内状态的穿甲独立算来源。
- 从装备获取的免伤上限为 30%。
- 功法、天赋、局内状态的免伤不受装备免伤上限影响。

### 招架和偏斜

- “闪避”改名为“偏斜”，避免玩家误解为完全无伤。
- 招架/偏斜触发后才有减伤，不触发就没有该项减伤。
- 招架和偏斜独立掷骰，但不能同时生效。
- 如果两者都判定成功，比较 `骰子值 / 概率值`，数值更小的那个触发。
- 这样堆招架值会提高招架实际出现倾向，堆偏斜同理。

当前公式：

```text
招架率 =
受击者招架 / (受击者招架 + 攻击者速度 * 1.5 + 攻击者等级 * 10 + 500)

偏斜率 =
受击者偏斜 / (受击者偏斜 + 攻击者命中 * 1.5 + 攻击者等级 * 10 + 500)

招架免伤 =
20% + max(0, 受击者根骨 - 攻击者臂力) * 0.1%

偏斜免伤 =
20% + max(0, 受击者身法 - 攻击者身法) * 0.1%
```

## 抵抗与韧性

- 抵抗主要减免精神类控制效果：
  - 嘲讽
  - 沉默
  - 恐惧
  - 缴械等
- 韧性主要减免物理类控制效果：
  - 眩晕
  - 定身
  - 击退等
- 根骨提升韧性，比例 `1:1`。
- 定力提升抵抗，比例 `1:2.5`。
- 定力不加韧性。

## 当前验证状态

- 本地没有全局 `tsc`。
- 使用 `npx --yes -p typescript@5.5.4 tsc --noEmit` 可以做检查。
- 当前检查时，`RoleEquipmentPanel/RolePanelConfig/RolePanelTypes` 相关改动未出现新增错误。
- 仍存在工程原有类型错误：
  - `CloseNodeButton.ts`
  - `OpenNodeButton.ts`
  - `SceneJumpButton.ts`
  - `DailySignInManager.ts`

