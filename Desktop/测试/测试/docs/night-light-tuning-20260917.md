# 灯光层次微调

修改前备份：`../../backups/night-light-tuning-20260917-111003`。

只调整 `tools/night-vision-settings.json` 并重新运行既有烘焙脚本：

| 参数 | 原值 | 新值 |
| --- | ---: | ---: |
| ambientIntensity | 0.40 | 0.25 |
| outerIntensity | 0.42 | 0.28 |
| coreWidth | 400 | 340 |

核心长度 600、核心强度 0.98、散射宽度 760、柔边参数和 512×512 纹理尺寸均保持原值。
没有新增光层、滤镜、运行时计算或修改固定缓存方式。

检查：

- city.ls 实际预览了调整前、调整后、临时隐藏两张光纹理只留黑夜遮罩的三个版本，起始位置与朝向相同。
- 隐藏光照后，地图分区的明暗差异仍存在；此次画面未发现上半屏被大范围光层额外照亮。不将地图自身的明暗分区归因于散射光过宽。
- 临时隐藏设置已恢复，场景与 DynamicCutoutProbe.ts 的 SHA256 与本次备份相同。
- 两张 PNG 的尺寸相同，边缘完全透明，关于光束中轴的 alpha 镜像一致，没有新增向上偏亮的渐变；相对备份，修改只降低或保持 alpha。
- 本次没有重新运行完整性能基准：只改变离线纹理像素，原先验证的运行时代码、Sprite 尺寸与缓存边界保持不变。

截图：调整前在备份目录的 `before.png`；调整后为 `../.tmp/night-light-tuned.png`；仅遮罩对比为 `../.tmp/night-light-mask-only.png`。
