/*
 * PlayerSaveData note placeholder.
 *
 * This file previously contained plain-text design notes rather than
 * TypeScript code, which blocked project-wide type checking. Keep it as
 * a valid module until real player save-data types are implemented.
 */

export {};
