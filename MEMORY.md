# Project Memory

## 操作习惯

- 用户布置好地点 prefab 后，先读取实际 prefab 内部的 `text`、`buildingName`、`displayName`，按现有地块文字接内容，不擅自改布局、不新增未确认地块。
- 如果 prefab 是复制模板，优先清理旧的 `buildingName/displayName` 残留，避免点击后串到其他地图内容。
- 多个地点可能有同名地块，例如 `校场`、`客栈`、`驿站`。内部点击数据使用 `地点/地块` 作为唯一 key，弹窗标题用 `displayName` 显示原地块名。
- 用户更偏好地块内显示职业或功能名，除核心人物外不要堆太多人名，避免列表显得乱。
- 用户明确说“只有某几个地块”时，只保留这些地块对应内容；不要补额外隐藏地点或备用人物。
- 先按用户给出的名称和设定执行；如果名称不一致，优先核对实际 prefab 文件名、地图数据和文档记录，再统一。
- 修改后需要检查 prefab JSON 是否能解析，并搜索确认旧绑定没有残留。
- 文档记录要跟代码同步，尤其是道域下属地点、门派归属和武学设定。

## 任务提示习惯

- 地图地点、内部地块、NPC 如果有任务/剧情状态，用统一标记提示玩家。
- `?`：可触发，有新剧情、新任务、新对话。
- `!`：任务进行中目标，需要玩家去处理。
- `◇`：已完成，可结算、领奖励或收尾。
- 空标记：普通交互。
- 主线标记优先用金色；支线可用蓝色；进行中目标用橙红色；完成待结算用青绿色或浅金色。
- 地图大节点和内部地块都要能透出任务状态：例如枫桥镇内武馆有任务时，上层枫桥镇节点也应显示对应标记。
